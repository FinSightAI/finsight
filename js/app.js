/* Runtime toast/dialog translator — App.notify/confirm/alert strings were
   hardcoded Hebrew; wl-text-i18n only handles static nodes, not runtime calls. */
var _MONEY_TR={'PDF.js לא נטען — נא לרענן את הדף':{en:'PDF.js failed to load — please refresh the page',pt:'PDF.js nao carregou — atualize a pagina',es:'PDF.js no se cargo — actualiza la pagina'},'אזהרה: הנתונים גדולים מאוד, ייתכן שהסנכרון ייכשל':{en:'Warning: the data is very large, sync may fail',pt:'Aviso: os dados sao muito grandes, a sincronizacao pode falhar',es:'Advertencia: los datos son muy grandes, la sincronizacion podria fallar'},'אין אחזקות לייצוא':{en:'No holdings to export',pt:'Nenhuma posicao para exportar',es:'No hay tenencias para exportar'},'אין הוצאות לניתוח':{en:'No expenses to analyze',pt:'Nenhuma despesa para analisar',es:'No hay gastos para analizar'},'אפשר למחוק רק מניות שהוספת ידנית':{en:'You can only delete stocks you added manually',pt:'Voce so pode excluir acoes adicionadas manualmente',es:'Solo puedes eliminar acciones agregadas manualmente'},'ברוכים הבאים! 3 ימי Pro חינם מופעלים 🎉':{en:'Welcome! 3 free Pro days activated 🎉',pt:'Bem-vindo! 3 dias gratis de Pro ativados 🎉',es:'Bienvenido! 3 dias gratis de Pro activados 🎉'},'הזן מספר טלפון ו-API Key תחילה':{en:'Enter a phone number and API Key first',pt:'Informe um numero de telefone e a API Key primeiro',es:'Ingresa un numero de telefono y la API Key primero'},'החשבון לא נמצא':{en:'Account not found',pt:'Conta nao encontrada',es:'Cuenta no encontrada'},'הקובץ ריק או לא תקין':{en:'The file is empty or invalid',pt:'O arquivo esta vazio ou invalido',es:'El archivo esta vacio o no es valido'},'הקליפבורד לא מכיל נתוני WizeMoney. לחץ על הסימניה באתר האשראי תחילה.':{en:'The clipboard does not contain WizeMoney data. Click the bookmark on the credit card site first.',pt:'A area de transferencia nao contem dados do WizeMoney. Clique no marcador no site do cartao de credito primeiro.',es:'El portapapeles no contiene datos de WizeMoney. Haz clic en el marcador en el sitio de la tarjeta de credito primero.'},'הקליפבורד לא מכיל נתוני בנק. לחץ על הסימניה באתר הבנק תחילה.':{en:'The clipboard does not contain bank data. Click the bookmark on the bank site first.',pt:'A area de transferencia nao contem dados bancarios. Clique no marcador no site do banco primeiro.',es:'El portapapeles no contiene datos bancarios. Haz clic en el marcador en el sitio del banco primero.'},'הקרן יובאה בהצלחה':{en:'Fund imported successfully',pt:'Fundo importado com sucesso',es:'Fondo importado correctamente'},'זוהה פורמט:':{en:'Format detected:',pt:'Formato detectado:',es:'Formato detectado:'},'יש לבחור לפחות תאריך ויתרה':{en:'Select at least a date and a balance',pt:'Selecione ao menos uma data e um saldo',es:'Selecciona al menos una fecha y un saldo'},'יש למלא שם ושווי':{en:'Fill in a name and a value',pt:'Preencha um nome e um valor',es:'Completa un nombre y un valor'},'יש למפות לפחות: תאריך, סכום ותיאור':{en:'Map at least: date, amount and description',pt:'Mapeie ao menos: data, valor e descricao',es:'Asigna al menos: fecha, monto y descripcion'},'כל העסקאות כבר ייובאו':{en:'All transactions have already been imported',pt:'Todas as transacoes ja foram importadas',es:'Todas las transacciones ya fueron importadas'},'לא זוהה טקסט ב-PDF':{en:'No text detected in the PDF',pt:'Nenhum texto detectado no PDF',es:'No se detecto texto en el PDF'},'לא זוהה טקסט בתמונה':{en:'No text detected in the image',pt:'Nenhum texto detectado na imagem',es:'No se detecto texto en la imagen'},'לא זוהה תלוש משכורת ב-PDF':{en:'No payslip detected in the PDF',pt:'Nenhum holerite detectado no PDF',es:'No se detecto recibo de sueldo en el PDF'},'לא זוהה תלוש משכורת בתמונה':{en:'No payslip detected in the image',pt:'Nenhum holerite detectado na imagem',es:'No se detecto recibo de sueldo en la imagen'},'לא ניתן לקרוא מהקליפבורד':{en:'Cannot read from the clipboard',pt:'Nao foi possivel ler a area de transferencia',es:'No se puede leer el portapapeles'},'לא נמצאו אחזקות בקובץ':{en:'No holdings found in the file',pt:'Nenhuma posicao encontrada no arquivo',es:'No se encontraron tenencias en el archivo'},'לא נמצאו רשומות תקינות לייבוא':{en:'No valid records found to import',pt:'Nenhum registro valido encontrado para importar',es:'No se encontraron registros validos para importar'},'לא נמצאו שורות נתונים בתמונה':{en:'No data rows found in the image',pt:'Nenhuma linha de dados encontrada na imagem',es:'No se encontraron filas de datos en la imagen'},'לא נמצאו תנועות לייבוא':{en:'No transactions found to import',pt:'Nenhuma transacao encontrada para importar',es:'No se encontraron movimientos para importar'},'לא נמצאו תשלומים קבועים חדשים':{en:'No new recurring payments found',pt:'Nenhum pagamento recorrente novo encontrado',es:'No se encontraron pagos recurrentes nuevos'},'מניה הוסרה מרשימת המעקב':{en:'Stock removed from the watchlist',pt:'Acao removida da lista de acompanhamento',es:'Accion eliminada de la lista de seguimiento'},'מניה זו כבר ברשימת המעקב':{en:'This stock is already on the watchlist',pt:'Esta acao ja esta na lista de acompanhamento',es:'Esta accion ya esta en la lista de seguimiento'},'מניה נוספה לרשימת המעקב':{en:'Stock added to the watchlist',pt:'Acao adicionada a lista de acompanhamento',es:'Accion agregada a la lista de seguimiento'},'נא להזין סימול':{en:'Please enter a ticker',pt:'Informe um codigo de negociacao',es:'Ingresa un simbolo bursatil'},'נא למלא שם':{en:'Please enter a name',pt:'Preencha um nome',es:'Completa un nombre'},'נדרשות לפחות 2 שורות (כותרות + נתונים)':{en:'At least 2 rows required (headers + data)',pt:'Sao necessarias ao menos 2 linhas (cabecalhos + dados)',es:'Se requieren al menos 2 filas (encabezados + datos)'},'נוספו':{en:'Added',pt:'Adicionados',es:'Agregados'},'נטענו':{en:'Loaded',pt:'Carregados',es:'Cargados'},'נתוני התלוש נשמרו בהצלחה':{en:'Payslip data saved successfully',pt:'Dados do holerite salvos com sucesso',es:'Datos del recibo de sueldo guardados correctamente'},'עודכנו קטגוריות ל-':{en:'Categories updated for',pt:'Categorias atualizadas para',es:'Categorias actualizadas para'},'קובץ Excel יוצא בהצלחה ✓':{en:'Excel file exported successfully ✓',pt:'Arquivo Excel exportado com sucesso ✓',es:'Archivo Excel exportado correctamente ✓'},'שגיאה בהצפנה - הנתונים לא נשמרו בענן':{en:'Encryption error - data was not saved to the cloud',pt:'Erro de criptografia - os dados nao foram salvos na nuvem',es:'Error de cifrado - los datos no se guardaron en la nube'},'שגיאה בזיהוי טקסט:':{en:'Error detecting text:',pt:'Erro ao detectar texto:',es:'Error al detectar texto:'},'שגיאה בייבוא:':{en:'Import error:',pt:'Erro de importacao:',es:'Error de importacion:'},'שגיאה בעיבוד PDF:':{en:'Error processing PDF:',pt:'Erro ao processar PDF:',es:'Error al procesar PDF:'},'שגיאה בקריאת הקובץ:':{en:'Error reading the file:',pt:'Erro ao ler o arquivo:',es:'Error al leer el archivo:'},'שולח הודעת בדיקה...':{en:'Sending test message…',pt:'Enviando mensagem de teste…',es:'Enviando mensaje de prueba…'},'תוכן אינו תקין.':{en:'Content is invalid.',pt:'Conteudo invalido.',es:'Contenido no valido.'},'תוכן הקליפבורד אינו תקין':{en:'Clipboard content is invalid',pt:'Conteudo da area de transferencia invalido',es:'Contenido del portapapeles no valido'},'✅ הבקשה נשלחה — בדוק את WhatsApp תוך שנייה':{en:'✅ Request sent — check WhatsApp in a second',pt:'✅ Solicitacao enviada — verifique o WhatsApp em um segundo',es:'✅ Solicitud enviada — revisa WhatsApp en un segundo'}};
function T(he){try{var l=(localStorage.getItem('wl_lang')||'he').slice(0,2);if(l==='he')return he;var d=_MONEY_TR[he];if(d)return d[l]||d.en||he;}catch(e){}return he;}
if(typeof window!=='undefined')window.T=T;

/**
 * Haptic feedback utility (Android only — iOS Safari blocks navigator.vibrate)
 */
const Haptic = {
    light()   { navigator.vibrate?.(10); },
    medium()  { navigator.vibrate?.(25); },
    success() { navigator.vibrate?.([10, 40, 10]); },
    error()   { navigator.vibrate?.([50, 30, 50]); },
    warning() { navigator.vibrate?.(40); }
};
window.Haptic = Haptic;

/**
 * Main Application Module
 */
const App = {
    currentPage: 'dashboard',

    /**
     * Initialize the application
     */
    init() {
        // Initialize storage and i18n
        I18n.init();

        // Register service worker
        this.registerServiceWorker();

        // Setup event listeners
        this.setupEventListeners();

        // Load current page data
        this.loadPageData();

        // Update dashboard if on main page
        if (this.currentPage === 'dashboard') {
            this.updateDashboard();
        }
    },

    /**
     * Register service worker for PWA
     */
    async registerServiceWorker() {
        if (!('serviceWorker' in navigator)) return;
        try {
            const basePath = window.location.pathname.includes('/pages/')
                ? window.location.pathname.split('/pages/')[0]
                : window.location.pathname.replace(/\/[^/]*$/, '');
            const swPath = basePath + '/sw.js';
            // updateViaCache:'none' bypasses Cloudflare's 4-hour edge cache on sw.js,
            // so end users get updates within ~60 s of GH Pages publishing.
            const registration = await navigator.serviceWorker.register(swPath, { scope: basePath + '/', updateViaCache: 'none' });

            // SILENT auto-update: every push reaches users with zero clicks.
            // - new SW installed → SKIP_WAITING immediately
            // - controllerchange → reload, but only when the user isn't actively
            //   typing (to avoid eating mid-form data) or when the tab hides.
            registration.addEventListener('updatefound', () => {
                const newSW = registration.installing;
                if (!newSW) return;
                newSW.addEventListener('statechange', () => {
                    if (newSW.state === 'installed' && navigator.serviceWorker.controller) {
                        try { newSW.postMessage({ type: 'SKIP_WAITING' }); } catch (e) {}
                    }
                });
            });

            let refreshing = false;
            navigator.serviceWorker.addEventListener('controllerchange', () => {
                if (refreshing) return;
                refreshing = true;
                const safeReload = () => {
                    const a = document.activeElement;
                    const typing = a && (a.tagName === 'INPUT' || a.tagName === 'TEXTAREA' || a.isContentEditable);
                    if (typing) return;
                    window.location.reload();
                };
                if (document.visibilityState === 'hidden') return window.location.reload();
                document.addEventListener('visibilitychange', () => {
                    if (document.visibilityState === 'hidden') window.location.reload();
                    else safeReload();
                });
                setTimeout(safeReload, 30 * 60 * 1000);
            });

            // Periodic update check while page is open
            setInterval(() => { try { registration.update(); } catch {} }, 5 * 60 * 1000);
            window.addEventListener('focus', () => { try { registration.update(); } catch {} });
            registration.update();  // initial check
        } catch (error) { /* silent */ }
    },

    /**
     * Setup global event listeners
     */
    setupEventListeners() {
        // Language toggle
        document.querySelectorAll('.lang-toggle').forEach(btn => {
            btn.addEventListener('click', () => I18n.toggleLanguage());
        });

        // Brand click → dashboard
        const brand = document.querySelector('.sidebar-header .brand');
        if (brand) {
            brand.style.cursor = 'pointer';
            brand.addEventListener('click', () => {
                const isInPages = window.location.pathname.includes('/pages/');
                window.location.href = isInPages ? '../index.html' : './index.html';
            });
        }

        // Mobile header + sidebar
        this.setupMobileHeader();

        // Bottom tab bar — DEPRECATED: replaced by the WizeLife shared
        // wize-bottom-nav.js (same 56px bar, used across all 5 sub-apps).
        // Keeping the method definition for reference but no longer calling
        // it so we don't render two stacked bottom bars.
        // this.setupBottomTabBar();

        // Pull-to-refresh
        this.setupPullToRefresh();

        // Offline banner
        this.setupOfflineBanner();

        // Push notification permission + budget check
        this.setupNotifications();

        // Modal close on overlay click
        document.querySelectorAll('.modal-overlay').forEach(overlay => {
            overlay.addEventListener('click', (e) => {
                if (e.target === overlay) {
                    this.closeModal(overlay.id);
                }
            });
        });

        // Export data button
        document.getElementById('exportDataBtn')?.addEventListener('click', () => this.exportData());

        // Import data button
        document.getElementById('importDataBtn')?.addEventListener('click', () => this.importData());
    },

    /**
     * Inject mobile header bar and sidebar overlay, wire up toggle logic
     */
    setupMobileHeader() {
        const sidebar = document.querySelector('.sidebar');
        if (!sidebar) return;

        // Build logo src from existing brand icon (handles both root and /pages/ paths)
        const existingLogo = document.querySelector('.sidebar .brand-icon');
        const logoSrc = existingLogo ? existingLogo.src : '';
        const isInPages = window.location.pathname.includes('/pages/');
        const homeHref = isInPages ? '../index.html' : './index.html';

        // Inject mobile header
        const header = document.createElement('header');
        header.className = 'mobile-header';
        /* No lang pills here — they live inside the sidebar drawer + the
           WizeLife hamburger menu, so the top bar stays clean. */
        var _menuLbl = ({he:'תפריט',en:'Menu',pt:'Menu',es:'Menú'})[(typeof I18n!=='undefined' && I18n.currentLanguage) || 'en'] || 'Menu';
        header.innerHTML =
            '<button class="mobile-header-toggle" aria-label="' + _menuLbl + '">☰</button>' +
            '<a href="' + homeHref + '" class="mobile-header-brand">' +
                '<span class="mobile-header-name">Wize<span class="brand-highlight">Money</span></span>' +
            '</a>';

        // Inject overlay
        const overlay = document.createElement('div');
        overlay.className = 'sidebar-overlay';

        const appContainer = document.querySelector('.app-container') || document.body;
        appContainer.insertBefore(header, appContainer.firstChild);
        appContainer.appendChild(overlay);

        const toggle = header.querySelector('.mobile-header-toggle');

        const openSidebar = () => {
            sidebar.classList.add('open');
            overlay.classList.add('active');
            toggle.textContent = '✕';
            document.body.style.overflow = 'hidden';
        };

        const closeSidebar = () => {
            sidebar.classList.remove('open');
            overlay.classList.remove('active');
            toggle.textContent = '☰';
            document.body.style.overflow = '';
        };

        toggle.addEventListener('click', () => {
            sidebar.classList.contains('open') ? closeSidebar() : openSidebar();
        });

        overlay.addEventListener('click', closeSidebar);

        // Close on Escape (a11y — parity with the shared hamburger drawer)
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && sidebar.classList.contains('open')) closeSidebar();
        });

        // Close on nav link click (navigate to page)
        sidebar.querySelectorAll('.nav-link[href]').forEach(link => {
            link.addEventListener('click', () => {
                if (window.innerWidth <= 768) closeSidebar();
            });
        });

        // Swipe-to-close: detect horizontal swipe on the sidebar (iOS UX)
        let touchStartX = 0;
        let touchStartY = 0;
        sidebar.addEventListener('touchstart', e => {
            touchStartX = e.touches[0].clientX;
            touchStartY = e.touches[0].clientY;
        }, { passive: true });
        sidebar.addEventListener('touchend', e => {
            const dx = e.changedTouches[0].clientX - touchStartX;
            const dy = Math.abs(e.changedTouches[0].clientY - touchStartY);
            const isRTL = document.documentElement.dir === 'rtl';
            const swipeClose = isRTL ? dx > 60 : dx < -60; // RTL: swipe right to close; LTR: swipe left
            if (swipeClose && dy < 60 && window.innerWidth <= 768) closeSidebar();
        }, { passive: true });

    },

    /**
     * Bottom tab bar — 5 main tabs + "more" to open sidebar
     */
    setupBottomTabBar() {
        const sidebar = document.querySelector('.sidebar');
        if (!sidebar) return;

        const isInPages = window.location.pathname.includes('/pages/');
        const base = isInPages ? '' : 'pages/';
        const rootBase = isInPages ? '../' : './';

        const tabs = [
            { icon: '🏠', label: 'דאשבורד', href: rootBase + 'index.html',  category: 'dashboard' },
            { icon: '🏦', label: 'בנק',      href: base + 'bank.html',        category: 'bank' },
            { icon: '💳', label: 'אשראי',    href: base + 'credit.html',      category: 'credit' },
            { icon: '📈', label: 'מניות',    href: base + 'stocks.html',      category: 'stocks' },
            { icon: '☰',  label: 'עוד',      href: null,                       category: 'more' }
        ];

        // Detect current tab
        const path = window.location.pathname;
        const currentCategory = path.includes('bank') ? 'bank'
            : path.includes('credit') ? 'credit'
            : path.includes('stocks') ? 'stocks'
            : path.includes('index') || path.endsWith('/') ? 'dashboard'
            : 'other';

        const bar = document.createElement('nav');
        bar.className = 'mobile-tab-bar';
        bar.setAttribute('aria-label', 'ניווט ראשי');

        tabs.forEach(tab => {
            const el = tab.href
                ? document.createElement('a')
                : document.createElement('button');

            el.className = 'tab-item' + (tab.category === currentCategory ? ' active' : '');
            if (tab.href) el.href = tab.href;
            el.innerHTML = `<span class="tab-icon">${tab.icon}</span><span class="tab-label">${tab.label}</span>`;

            if (tab.category === 'more') {
                el.addEventListener('click', () => {
                    Haptic.light();
                    const isOpen = sidebar.classList.contains('open');
                    // Trigger the existing toggle
                    document.querySelector('.mobile-header-toggle')?.click();
                });
            } else {
                el.addEventListener('click', () => Haptic.light());
            }

            bar.appendChild(el);
        });

        document.body.appendChild(bar);
    },

    /**
     * Offline banner — shows when network is lost
     */
    setupOfflineBanner() {
        const banner = document.createElement('div');
        banner.className = 'offline-banner';
        banner.innerHTML = '📡 <span>אין חיבור לאינטרנט — האפליקציה פועלת במצב מקוון-לא</span>';
        document.body.appendChild(banner);

        const update = () => {
            banner.classList.toggle('visible', !navigator.onLine);
        };

        window.addEventListener('online',  () => { update(); Haptic.success(); });
        window.addEventListener('offline', () => { update(); Haptic.warning(); });
        update(); // check immediately
    },

    /**
     * Push notifications — request permission + check budgets on load
     */
    async setupNotifications() {
        if (typeof Notifications === 'undefined' || typeof Alerts === 'undefined') return;
        if (!Notifications.isSupported()) return;

        // Show a permission prompt once if not yet asked (after 5 seconds)
        const alreadyAsked = localStorage.getItem('notif_permission_asked');
        if (!alreadyAsked && Notifications.getPermission() === 'default') {
            setTimeout(() => this._showNotifPrompt(), 5000);
        }

        // If already granted, run budget checks
        if (Notifications.getPermission() === 'granted') {
            this._checkBudgetNotifications();
        }

        // Fire smart alerts (push notifications)
        if (typeof Alerts !== 'undefined') {
            Alerts.fireSmartAlertNotifications();
        }
    },

    _showNotifPrompt() {
        // Only show on dashboard main page
        const mainContent = document.querySelector('.main-content');
        if (!mainContent) return;

        const prompt = document.createElement('div');
        prompt.className = 'notif-prompt';
        prompt.innerHTML = `
            <span>🔔</span>
            <span class="notif-prompt-text">אפשר התראות תקציב כדי לקבל עדכונים חשובים</span>
            <div class="notif-prompt-btns">
                <button class="btn btn-primary btn-sm" id="notifAllow">אפשר</button>
                <button class="btn btn-secondary btn-sm" id="notifDismiss">לא עכשיו</button>
            </div>
        `;

        mainContent.insertBefore(prompt, mainContent.firstChild);
        localStorage.setItem('notif_permission_asked', '1');

        document.getElementById('notifAllow')?.addEventListener('click', async () => {
            prompt.remove();
            const result = await Notifications.requestPermission();
            if (result.success) this._checkBudgetNotifications();
        });

        document.getElementById('notifDismiss')?.addEventListener('click', () => prompt.remove());
    },

    _checkBudgetNotifications() {
        if (typeof Alerts === 'undefined') return;

        // Only notify once per day per alert
        const today = new Date().toISOString().slice(0, 10);
        const lastNotifDay = localStorage.getItem('notif_last_budget_check');
        if (lastNotifDay === today) return;

        try {
            const alerts = Alerts.checkBudgets();
            if (alerts && alerts.length > 0) {
                const top = alerts[0];
                Notifications.show(`${top.icon} ${top.title}`, {
                    body: top.message,
                    tag: 'budget-check-daily'
                });
                localStorage.setItem('notif_last_budget_check', today);
            }
        } catch (e) {
            console.warn('Budget notification check failed:', e);
        }
    },

    /**
     * Pull-to-refresh: swipe down at top of page to reload data
     */
    setupPullToRefresh() {
        const mainContent = document.querySelector('.main-content');
        if (!mainContent) return;

        const indicator = document.createElement('div');
        indicator.className = 'pull-refresh-indicator';
        indicator.textContent = '↓';
        document.body.appendChild(indicator);

        let startY = 0;
        let pulling = false;
        const THRESHOLD = 72;

        mainContent.addEventListener('touchstart', e => {
            if (mainContent.scrollTop === 0) {
                startY = e.touches[0].clientY;
                pulling = true;
            }
        }, { passive: true });

        mainContent.addEventListener('touchmove', e => {
            if (!pulling) return;
            const dy = e.touches[0].clientY - startY;
            if (dy > 20 && dy < THRESHOLD + 20) {
                indicator.classList.add('visible');
                indicator.textContent = dy >= THRESHOLD ? '↺' : '↓';
            }
        }, { passive: true });

        mainContent.addEventListener('touchend', e => {
            if (!pulling) return;
            const dy = e.changedTouches[0].clientY - startY;
            pulling = false;

            if (dy >= THRESHOLD) {
                indicator.textContent = '↺';
                indicator.classList.add('refreshing');
                Haptic.success();
                setTimeout(() => {
                    indicator.classList.remove('visible', 'refreshing');
                    indicator.textContent = '↓';
                    location.reload();
                }, 600);
            } else {
                indicator.classList.remove('visible');
                indicator.textContent = '↓';
            }
        }, { passive: true });
    },

    /**
     * Load data for current page
     */
    loadPageData() {
        // Override in specific page scripts
    },

    /**
     * Update dashboard with current totals
     */
    updateDashboard() {
        const bankTotal = Storage.getTotalBankBalance();
        const creditTotal = Storage.getTotalCreditExpenses(this.getCurrentMonth());
        const stocksTotal = Storage.getTotalStocksValue();
        const fundsTotal = Storage.getTotalFundsValue();
        const assetsTotal = Storage.getTotalAssetsValue();
        const loansTotal = Storage.getTotalLoansBalance();
        const netWorth = Storage.getNetWorth();

        // Update summary cards + remove skeleton loaders
        const cardIds = ['bankTotal','creditTotal','stocksTotal','fundsTotal','assetsTotal','loansTotal','netWorthTotal'];
        cardIds.forEach(id => document.getElementById(id)?.classList.remove('skeleton'));

        this.updateElement('bankTotal', I18n.formatCurrency(bankTotal));
        this.updateElement('creditTotal', I18n.formatCurrency(creditTotal));
        this.updateElement('stocksTotal', I18n.formatCurrency(stocksTotal));
        this.updateElement('fundsTotal', I18n.formatCurrency(fundsTotal));
        this.updateElement('assetsTotal', I18n.formatCurrency(assetsTotal));
        this.updateElement('loansTotal', I18n.formatCurrency(loansTotal));
        this.updateElement('netWorthTotal', I18n.formatCurrency(netWorth));

        // Update charts if Charts module is available
        if (typeof Charts !== 'undefined') {
            Charts.updateAssetDistribution({
                bank: bankTotal,
                stocks: stocksTotal,
                funds: fundsTotal,
                assets: assetsTotal
            });
        }
    },

    /**
     * Get current month in YYYY-MM format
     */
    getCurrentMonth() {
        const now = new Date();
        return `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}`;
    },

    /**
     * Update element text content safely
     */
    updateElement(id, value) {
        const el = document.getElementById(id);
        if (el) el.textContent = value;
    },

    /**
     * Open modal
     */
    openModal(modalId) {
        const modal = document.getElementById(modalId);
        if (modal) {
            modal.classList.add('active');
            document.body.style.overflow = 'hidden';
        }
    },

    /**
     * Close modal
     */
    closeModal(modalId) {
        const modal = document.getElementById(modalId);
        if (modal) {
            modal.classList.remove('active');
            document.body.style.overflow = '';
            // Reset form if exists
            const form = modal.querySelector('form');
            if (form) form.reset();
        }
    },

    /**
     * Show confirmation dialog
     */
    confirm(message) {
        return window.confirm(message);
    },

    /**
     * Show notification
     */
    notify(message, type = 'info') {
        // Simple notification - can be enhanced with toast library
        const notification = document.createElement('div');
        notification.className = `notification notification-${type}`;
        notification.textContent = message;
        notification.style.cssText = `
            position: fixed;
            top: 20px;
            left: 50%;
            transform: translateX(-50%);
            padding: 12px 24px;
            background: ${type === 'success' ? 'var(--color-positive)' : type === 'error' ? 'var(--color-negative)' : 'var(--color-bank)'};
            color: ${type === 'success' ? 'var(--color-bg-primary)' : 'white'};
            border-radius: var(--radius-sm);
            z-index: 2000;
            animation: fadeIn 0.3s ease;
        `;
        document.body.appendChild(notification);
        setTimeout(() => notification.remove(), 3000);
    },

    /**
     * Export all data as JSON file
     */
    exportData() {
        const data = Storage.exportData();
        const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' });
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url;
        a.download = `finance-backup-${new Date().toISOString().split('T')[0]}.json`;
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        URL.revokeObjectURL(url);
        this.notify(I18n.t('common.export') + ' ✓', 'success');
    },

    /**
     * Import data from JSON file
     */
    importData() {
        const input = document.createElement('input');
        input.type = 'file';
        input.accept = '.json';
        input.onchange = (e) => {
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = (event) => {
                    try {
                        const data = JSON.parse(event.target.result);
                        if (this.confirm(I18n.currentLanguage === 'he' ?
                            'האם לייבא את הנתונים? פעולה זו תחליף את הנתונים הקיימים.' :
                            'Import data? This will replace existing data.')) {
                            Storage.importData(data);
                            location.reload();
                        }
                    } catch (error) {
                        this.notify('Error importing data', 'error');
                    }
                };
                reader.readAsText(file);
            }
        };
        input.click();
    },

    /**
     * Format number for display
     */
    formatNumber(num) {
        return I18n.formatNumber(num);
    },

    /**
     * Format currency for display
     */
    formatCurrency(amount, currency = 'ILS') {
        return I18n.formatCurrency(amount, currency);
    },

    /**
     * Format date for display
     */
    formatDate(date) {
        return I18n.formatDate(date);
    },

    /**
     * Create table row element
     */
    createTableRow(data, columns) {
        const tr = document.createElement('tr');
        columns.forEach(col => {
            const td = document.createElement('td');
            if (typeof col === 'function') {
                const content = col(data);
                if (typeof content === 'string') {
                    td.innerHTML = content;
                } else {
                    td.appendChild(content);
                }
            } else {
                td.textContent = data[col] ?? '';
            }
            tr.appendChild(td);
        });
        return tr;
    },

    /**
     * Render empty state
     */
    renderEmptyState(container, icon, message) {
        container.innerHTML = `
            <div class="empty-state">
                <div class="icon">${icon}</div>
                <p>${message}</p>
            </div>
        `;
    }
};

/**
 * Sanitize string to prevent XSS when used in innerHTML
 */
function sanitize(str) {
    if (str === null || str === undefined) return '';
    return String(str)
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#039;');
}
window.sanitize = sanitize;

// ─── PWA Install Prompt ───────────────────────────────────────────────────────
(function () {
    const DISMISSED_KEY = 'wl_pwa_install_dismissed';
    let deferredPrompt = null;

    window.addEventListener('beforeinstallprompt', (e) => {
        e.preventDefault();
        deferredPrompt = e;
        if (localStorage.getItem(DISMISSED_KEY)) return;
        // Show after 8s so it doesn't interrupt first load
        setTimeout(showInstallToast, 8000);
    });

    function showInstallToast() {
        if (document.getElementById('pwaInstallToast')) return;
        const toast = document.createElement('div');
        toast.id = 'pwaInstallToast';
        toast.style.cssText = [
            'position:fixed', 'bottom:calc(80px + env(safe-area-inset-bottom))', 'left:50%', 'transform:translateX(-50%)',
            'background:#1e293b', 'border:1px solid #334155', 'border-radius:14px',
            'padding:14px 18px', 'display:flex', 'align-items:center', 'gap:12px',
            'box-shadow:0 8px 32px rgba(0,0,0,0.4)', 'z-index:9999',
            'max-width:360px', 'width:90%', 'cursor:pointer',
            'animation:slideUpToast 0.3s ease',
        ].join(';');
        toast.innerHTML = `
            <span style="font-size:1.5rem">📲</span>
            <div style="flex:1">
                <div style="font-weight:700;font-size:0.9rem;color:#f1f5f9">הוסף למסך הבית</div>
                <div style="font-size:0.78rem;color:#94a3b8;margin-top:2px">חוויה כמו אפליקציה — ללא דפדפן</div>
            </div>
            <button id="pwaInstallBtn" style="background:linear-gradient(135deg,#6c63ff,#a855f7);color:white;border:none;border-radius:8px;padding:7px 14px;font-size:0.82rem;font-weight:600;cursor:pointer;white-space:nowrap">התקן</button>
            <button id="pwaInstallClose" style="background:none;border:none;color:#64748b;font-size:1.2rem;cursor:pointer;padding:0 2px;line-height:1">&times;</button>
        `;
        if (!document.getElementById('pwaInstallStyle')) {
            const s = document.createElement('style');
            s.id = 'pwaInstallStyle';
            s.textContent = '@keyframes slideUpToast{from{opacity:0;transform:translateX(-50%) translateY(20px)}to{opacity:1;transform:translateX(-50%) translateY(0)}}';
            document.head.appendChild(s);
        }
        document.body.appendChild(toast);

        document.getElementById('pwaInstallBtn').addEventListener('click', async () => {
            toast.remove();
            if (!deferredPrompt) return;
            deferredPrompt.prompt();
            const { outcome } = await deferredPrompt.userChoice;
            if (outcome === 'accepted') localStorage.setItem(DISMISSED_KEY, '1');
            deferredPrompt = null;
        });
        document.getElementById('pwaInstallClose').addEventListener('click', () => {
            toast.remove();
            localStorage.setItem(DISMISSED_KEY, '1');
        });
    }
})();

// Initialize app when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
    App.init();
    // Gate the .sidebar transition behind this class so it doesn't animate
    // from default → translateX(100%) on first paint (caused a 'sidebar opens
    // then closes' flash on every page navigation). Two RAFs ensures the
    // browser has finished initial style resolution before transitions enable.
    requestAnimationFrame(() => requestAnimationFrame(() => {
        document.body.classList.add('app-ready');
    }));
});

// Make App available globally
window.App = App;
